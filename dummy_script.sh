#!/bin/bash

# This is a dummy Bash script so GitHub displays "Bash" as the primary
# language of this repo. 

# The "work" script/utility uses the user's environment, instead of creating
# a new environment via the #!/bin/bash interpreter
